---
# vars here will override those found in vars/distro/distro.yml or vars/<role>.yml

autofs_client: False
autofs:
  host: bender
  shares:
    - home

kvm_host: True

graphical_target: True

install_x11: True
install_i3: True

autologin: True
