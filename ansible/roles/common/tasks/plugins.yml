---

- name: set audio plugins packages to
  set_fact:
    _packages: "{{ audio.plugins  }}"
  tags: ['packages']

- name: Install audio plugins
  import_role:
    name: pkg_manager
  vars:
    pkg_state: present
  tags: ['packages']
