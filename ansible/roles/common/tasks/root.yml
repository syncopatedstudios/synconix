---

- name: set root shell to zsh
  user:
    name: root
    shell: /bin/zsh
    state: present
  tags: ['system', 'root']

# - name: push shell config to root
#   synchronize:
#     src: "root/{{ item }}"
#     dest: /root/
#     recursive: yes
#     mode: push
#     delete: no
#     checksum: yes
#     perms: yes
#     rsync_opts: "--update"
#   with_items:
#     - ".zsh"
#     - ".zshrc"
#     - ".zshenv"
#     - ".aliases"
#   tags: ['system', 'root']
#
# - name: ensure owner and group
#   command: chown -R root:root /root/
#   tags: ['system', 'root']
