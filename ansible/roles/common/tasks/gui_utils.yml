---

- name: set graphical audio utility packages to
  set_fact:
    _packages: "{{ audio['gutilities'] | join(',') }}"
  tags: ['packages']

- name: Install audio gui utilities
  import_role:
    name: pkg_manager
  vars:
    pkg_state: present
  tags: ['packages']
