---

- name: set audio tui utility packages
  set_fact:
    _packages: "{{ audio.utilities  }}"
  tags: ['packages']

- name: install audio tui utilities
  import_role:
    name: pkg_manager
  vars:
    pkg_state: present
  tags: ['packages']
