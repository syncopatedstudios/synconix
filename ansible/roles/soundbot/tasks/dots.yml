---

# - name: install dots from template
#   template:
#     src: usr/local/bin/dots.rb.j2
#     dest: /usr/local/bin/dots
#     owner: "{{ user.name }}"
#     group: "{{ user.group }}"
#     mode: '0755'
#     backup: yes
#   when: dots.home is defined

- block:
    - name: run dots
      shell: |
        /usr/local/bin/dots status
      args:
        chdir: "{{ user.home }}"

  become: True
  become_user: "{{ user.name }}"
  when: dots.home is defined
