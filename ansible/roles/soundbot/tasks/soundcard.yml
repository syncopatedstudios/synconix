---

- name: install modprobe configs for intel card
  copy:
    src: etc/modprobe.d/intel_powersave.conf
    dest: /etc/modprobe.d/intel_powersave.conf
    owner: root
    group: root
    mode: '0644'
    backup: yes
    directory_mode: no
