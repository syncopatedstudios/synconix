---

- name: set sound service packages to
  set_fact:
    _sound_services: "{{ (audio['alsa'] + audio['jack'] + audio['pulseaudio']) | difference(installed_packages) }}"
  tags: ['packages']

- name: set audio utility packages to
  set_fact:
    _audio_utils: "{{ audio['utilities'] | difference(installed_packages) }}"
  tags: ['packages']

- name: set graphical audio utility packages to
  set_fact:
    _audio_gutils: "{{ audio['gutilities']|difference(installed_packages) }}"
  tags: ['packages']

- name: set audio plugin packages to
  set_fact:
    _audio_plugins: "{{ audio['plugins']|difference(installed_packages) }}"
  tags: ['packages']

- include: pipewire.yml

- name: install sound services (alsa,jack,pulse)
  shell: "{{ ansible_pkg_mgr }} -y install {{ _sound_services | join(' ') }}"
  when: _sound_services | length > 0
  tags: ['packages']

- include: pulseaudio.yml

- name: push jack limits file
  copy:
    src: etc/security/limits.conf
    dest: /etc/security/limits.conf
    owner: root
    group: root
    mode: '0644'
  tags: ['tuning']

- name: Add user to audio group
  user:
    name: "{{ user.name }}"
    groups: audio
    append: yes
  tags: ['tuning']

- name: makesfz and makeh2drumkit
  copy:
    src: "usr/local/bin/{{ item }}"
    dest: "/usr/local/bin/{{ item }}"
    owner: root
    group: root
    mode: '0755'
  with_items:
    - makeh2kit
    - makesfz
  tags: ['soundfonts']

- name: install cli audio utilities
  shell: "{{ ansible_pkg_mgr }} -y install {{ _audio_utils | join(' ') }}"
  when: "install_audio_utilities|bool and _audio_utils | length > 0"
  tags: ['audio_utils', 'packages']

- name: install graphical audio utilities
  shell: "{{ ansible_pkg_mgr }} -y install {{ _audio_gutils | join(' ') }}"
  when: "install_audio_gutilities|bool and _audio_gutils | length > 0"
  tags: ['audio_gutils', 'packages']

- name: install plugins
  shell: "{{ ansible_pkg_mgr }} -y install {{ _audio_plugins | join(' ') }}"
  when: "install_audio_plugins|bool and _audio_plugins | length > 0"
  tags: ['audio_plugins', 'packages']

- include: ardour.yml
  tags: ['ardour']
  when: install_ardour|bool

- include: baudline.yml
  when: install_baudline|bool
  tags: ['baudline']

- include: deadbeef.yml
  when: install_deadbeef|bool
  tags: ['deadbeef']

- include: ecasound.yml
  when: install_ecasound|bool
  tags: ['ecasound']

- include: jacktrip.yml
  when: install_jacktrip|bool
  tags: ['jacktrip']

- include: jalvselect.yml
  when: install_raysession|bool
  tags: ['jalvselect']

- include: njconnect.yml
  when: install_njconnect|bool
  tags: ['njconnect']

- include: obs.yml
  when: install_obs|bool
  tags: ['obs']

- include: ocenaudio.yml
  when: "ocenaudio|bool and 'ocenaudio' not in installed_packages"
  tags: ['ocenaudio']

- include: qmidinet.yml
  when: install_qmidinet|bool
  tags: ['qmidinet']

- include: raysession.yml
  when: install_raysession|bool
  tags: ['raysession']

- include: reaper.yml
  when: install_reaper|bool
  tags: ['reaper']

- include: rtplugins.yml
  when: install_rtplugins|bool
  tags: ['rtplugins']

- include: soniclineup.yml
  when: install_soniclineup|bool
  tags: ['soniclineup']

- include: sonicpi.yml
  when: install_sonicpi|bool
  tags: ['sonicpi']

- include: sonicpitool.yml
  when: install_sonicpi|bool
  tags: ['sonicpitool']

- include: sonicvisualiser.yml
  when: install_sonicvisualiser|bool
  tags: ['sonicvisualiser']

- include: swami.yml
  when: install_swami|bool
  tags: ['swami']

- include: tony.yml
  when: install_tony|bool
  tags: ['tony']

- include: touchosc.yml
  when: install_touchosc|bool
  tags: ['TouchOSC']
