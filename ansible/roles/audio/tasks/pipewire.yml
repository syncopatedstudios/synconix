---

- name: remove pipewire packages
  shell: "{{ ansible_pkg_mgr }} -y remove {{ item }}"
  with_items: "{{ audio['pipewire'] }}"
  when: remove_pipewire|bool and item in installed_packages
  tags: ['packages']
