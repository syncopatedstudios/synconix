---

- name: set x11 packages to install
  set_fact:
    _ui_x11: "{{ ui['x11']|difference(installed_packages) }}"
  tags: ['packages']

- name: Install x11 stuff
  shell: "{{ ansible_pkg_mgr }} -y install {{ _ui_x11 | join(' ') }}"
  when: _ui_x11 | length > 0
  tags: ['packages']
