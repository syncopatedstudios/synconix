---

- name: set x11 packages to install
  set_fact:
    _packages: "{{ ui.x11  }}"
  tags: ['packages']

- name: Install x11 packages
  import_role:
    name: pkg_manager
  vars:
    pkg_state: present
  tags: ['packages']
