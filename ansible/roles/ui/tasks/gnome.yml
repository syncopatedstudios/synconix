---

- name: set packages to install
  set_fact:
    _ui_gnome: "{{ ui['gnome']|difference(installed_packages) }}"
  tags: ['packages']

- name: Install Gnome Stuff
  shell: "{{ ansible_pkg_mgr }} -y install {{ _ui_gnome | join(' ') }}"
  tags: ['packages']
