---
# vars here will override those found in host_vars/<host>.yml

apt_update: False

autofs_configs: /etc
admin_user: sudo
rtirq_dir: /etc/default/rtirq

grub_config: /boot/grub2/grub.cfg
grub_cmd: grub2-mkconfig
grub_set_default: grub2-set-default
