{% if ansible_facts['hostname'] == 'lapbot' %}
MODULES=(crc32c-intel btrfs)
{% else %}
{% endif %}
BINARIES=(/usr/bin/btrfs)
FILES=()
HOOKS=(base udev autodetect keyboard keymap modconf block filesystems fsck mdadm_udev)
