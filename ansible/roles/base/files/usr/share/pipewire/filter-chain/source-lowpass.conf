# Noise canceling source
#
# start with pipewire -c filter-chain/source-rnnoise.conf
#
context.properties = {
    log.level = 0
}

context.spa-libs = {
    audio.convert.* = audioconvert/libspa-audioconvert
    support.*       = support/libspa-support
}

context.modules = [
    { name = libpipewire-module-rt
        args = {
            #rt.prio      = 88
            #rt.time.soft = -1
            #rt.time.hard = -1
        }
        flags = [ ifexists nofail ]
    }
    { name = libpipewire-module-protocol-native }
    { name = libpipewire-module-client-node }
    { name = libpipewire-module-adapter }

    { name = libpipewire-module-filter-chain
        args = {
            node.description = "Glame Lowpass Filter"
            media.name       = "Glame Lowpass Filter"
            filter.graph = {
                nodes = [
                    {
                        type   = ladspa
                        name   = lowpass
                        plugin = lowpass_iir_1891
                        label  = lowpass_iir
                        control = {
                            "Cutoff Frequency" 18500.00
                            "Stages(2 poles per stage)" 3
                        }
                    }
                ]
            }
            audio.position = [ FL FR ]
            capture.props = {
                node.name = "effect_input.lowpass"
                node.passive = true
            }
            playback.props = {
                node.name = "effect_output.lowpass"
                media.class = Audio/Source
            }
        }
    }
]
