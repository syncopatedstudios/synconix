---

- name: permit traffic to qbittorrent ports
  firewalld:
    port: "{{ item }}"
    permanent: yes
    state: enabled
  notify:
    - "reload firewalld"
  with_items: ["{{ qbittorrent }}"]
  when: ansible_facts['hostname'] == 'bender'
