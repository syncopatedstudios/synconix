uid = {{ user.uid }}
gid = {{ user.gid }}
use chroot = no
max connections = 0
syslog facility = local5
pid file = /run/rsyncd.pid

[backups]
  path = /home/backups
  comment = backups
  read only = false
