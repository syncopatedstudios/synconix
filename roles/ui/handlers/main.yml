---

- name: update desktop database
  command: bash -lc "update-desktop-database"

- name: restart i3
  command: bash -lc "i3-msg restart"
  become: True
  become_user: "{{ user.name }}"

- name: reload systemd
  systemd:
    daemon_reload: yes
  become: True
  become_user: "{{ user.name }}"
