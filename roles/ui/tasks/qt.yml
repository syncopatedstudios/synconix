---

- name: set qt packages to install
  set_fact:
    _packages: "{{ ui.qt  }}"
  tags: ['packages']

- name: Install qt packages
  import_role:
    name: pkg_manager
  vars:
    pkg_state: present
  tags: ['packages']
