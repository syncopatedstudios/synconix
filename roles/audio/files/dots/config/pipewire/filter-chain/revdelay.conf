context.properties = {
    log.level        = 0
}

context.spa-libs = {
    audio.convert.* = audioconvert/libspa-audioconvert
    support.*       = support/libspa-support
}

context.modules = [

    { name = libpipewire-module-rtkit
        args = {
            #nice.level   = -11
            #rt.prio      = 88
            #rt.time.soft = 2000000
            #rt.time.hard = 2000000
        }
        flags = [ ifexists nofail ]
    }

    { name = libpipewire-module-protocol-native }
    { name = libpipewire-module-client-node }
    { name = libpipewire-module-adapter }

    { name = libpipewire-module-filter-chain
        args = {
            # audio.channels   = 1
            node.name        = "filter-chain-reverse"
            node.description = "reverse pitch"
            media.name       = "reverse pitch"
            filter.graph = {
                nodes = [
                    # {
                    #     type   = ladspa
                    #     name   = pitch
                    #     plugin = am_pitchshift_1433
                    #     label  = amPitchshift
                    #     control = {
                    #         "Pitch shift" = 0.6
                    #     }
                    # }
                    {
                        type   = ladspa
                        name   = rev
                        plugin = revdelay_1605
                        label  = revdelay
                        control = {
                            "Delay Time (s)" = 2.0
                        }
                    }
                ]

            }
              # links = [
              #     { output = "pitch:Output"   input = "rev:Input" }
              # ]
            # inputs = [ "rev:Input" ]
            # outputs = [ "pitch:Output"]

            capture.props = {
                media.class    = Audio/Sink
                # audio.channels = 1
                # node.passive = true
            }
            playback.props = {
                #media.class   = Audio/Source
                node.passive  = true
                # audio.channels = 1
            }
        }
    }
]
